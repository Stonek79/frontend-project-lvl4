const charactersLength = {
  minLength: 3,
  minPassLength: 6,
  maxLength: 20,
  messageMax: 400,
};

const modalTypes = {
  add: 'adding',
  rename: 'renaming',
  remove: 'removing',
};

export { charactersLength, modalTypes };
