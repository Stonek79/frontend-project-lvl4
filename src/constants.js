const itemsLength = {
  maxLength: 20,
  messageMax: 400,
  minLength: 3,
  minPassLength: 6,
};

const modalTypes = {
  add: 'adding',
  remove: 'removing',
  rename: 'renaming',
};

export { itemsLength, modalTypes };
