const charactersLength = {
  minLength: 3,
  maxLength: 17,
  messageMax: 400,
};

const modalTypes = {
  add: 'adding',
  rename: 'renaming',
  remove: 'removing',
};

export { charactersLength, modalTypes };
